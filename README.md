# sql-university-project
This project is a simple University Course Management System built using SQL.  It includes tables like students, courses, instructors, enrollments, and departments.  The project demonstrates SQL concepts such as joins, subqueries, aggregate functions, and CRUD operations.
